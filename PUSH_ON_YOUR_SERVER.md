# 🚀 Как перенести push-уведомления на свой сервер

## Файл с функцией: `SEND_PUSH_STANDALONE.py`

Это **standalone функция** которая работает независимо от poehali.dev.

---

## Шаг 1: Получить Firebase Server Key

1. Открой [Firebase Console](https://console.firebase.google.com/)
2. Выбери свой проект (или создай новый)
3. **Project Settings** → **Cloud Messaging**
4. Найди **"Server key"** (Cloud Messaging API - Legacy)
5. Скопируй этот ключ

---

## Шаг 2: Установить зависимости на сервере

```bash
pip install requests
```

Это всё что нужно! Функция использует только стандартную библиотеку `requests`.

---

## Шаг 3: Добавить функцию в свой код

### Вариант A: Скопировать функцию напрямую

Открой `SEND_PUSH_STANDALONE.py` и скопируй функцию `send_push_notification()` в свой проект.

**Не забудь заменить:**
```python
FIREBASE_SERVER_KEY = 'YOUR_FIREBASE_SERVER_KEY_HERE'
```

### Вариант B: Использовать как отдельный модуль

```python
# Импортируй функцию
from SEND_PUSH_STANDALONE import send_push_notification

# Используй где угодно
send_push_notification(
    fcm_token='user_device_token',
    title='Новое сообщение',
    body='Привет!',
    data={'chatUrl': '/chat/123'}
)
```

---

## Шаг 4: Интегрировать в свою функцию отправки сообщений

### Пример для любого фреймворка:

```python
def send_message_handler(sender_id, receiver_id, message_text):
    # 1. Сохраняем сообщение в БД
    save_message_to_db(sender_id, receiver_id, message_text)
    
    # 2. Получаем данные получателя из БД
    receiver = get_user_from_db(receiver_id)
    sender = get_user_from_db(sender_id)
    
    # 3. Если у получателя есть FCM токен - отправляем push
    if receiver.get('fcm_token'):
        send_push_notification(
            fcm_token=receiver['fcm_token'],
            title=f"💬 {sender['username']}",
            body=message_text[:50],  # Первые 50 символов
            data={
                'chatUrl': f'/chat/{sender_id}',
                'senderId': str(sender_id)
            }
        )
    
    return {'success': True}
```

---

## Шаг 5: Убедиться что FCM токены сохраняются

### В БД должна быть колонка:
```sql
ALTER TABLE users ADD COLUMN fcm_token TEXT;
```

### При авторизации сохранять токен:
```python
# После логина на фронтенде получаем FCM токен
fcm_token = await firebase.getToken()

# Отправляем на сервер
POST /save-fcm-token
{
  "user_id": 123,
  "fcm_token": "eXAMPLEtoken123..."
}

# На сервере сохраняем в БД
UPDATE users SET fcm_token = ? WHERE id = ?
```

---

## Типы уведомлений

### 1. Текстовое сообщение:
```python
send_push_notification(
    fcm_token=token,
    title='💬 Юра',
    body='Привет! Как дела?',
    data={'chatUrl': '/chat/7'}
)
```

### 2. Голосовое сообщение:
```python
send_push_notification(
    fcm_token=token,
    title='💬 Юра',
    body='🎤 Голосовое сообщение',
    data={'chatUrl': '/chat/7'}
)
```

### 3. Изображение:
```python
send_push_notification(
    fcm_token=token,
    title='💬 Юра',
    body='📷 Изображение',
    data={'chatUrl': '/chat/7'}
)
```

---

## Отладка

### Проверить что функция работает:
```python
# Тестовый вызов
result = send_push_notification(
    fcm_token='YOUR_TEST_TOKEN',
    title='Test',
    body='Testing push',
    data={}
)
print(f'Success: {result}')
```

### Логи Firebase:
- [Firebase Console → Cloud Messaging → Reports](https://console.firebase.google.com/)
- Смотри статистику отправленных уведомлений

### Частые ошибки:

❌ **"Invalid Server Key"**
- Проверь что скопировал правильный Server Key
- Должен начинаться с "AAAA..."

❌ **"Invalid FCM token"**
- Токен истёк или неправильный
- Попроси пользователя перезайти в приложение

❌ **"success": 0**
- Токен больше не валиден
- Удали токен из БД, пусть пользователь получит новый

---

## Готовые примеры интеграции

В файле `SEND_PUSH_STANDALONE.py` есть примеры для:
- ✅ Flask
- ✅ FastAPI
- ✅ Django
- ✅ Чистый PostgreSQL

Выбери свой фреймворк и следуй примеру!

---

## Где сейчас работает push в твоём проекте

**Файл:** `backend/private-messages/index.py` (строки 253-290)

Код автоматически отправляет push когда:
- Пользователь A отправляет сообщение пользователю B
- У пользователя B есть сохранённый `fcm_token` в БД
- Пользователь B не заблокировал пользователя A

**URL функции на poehali.dev:**
`https://functions.poehali.dev/78814097-be24-4f14-96b8-669fcaaf2e05`

Можешь скопировать логику из этого файла на свой сервер!
