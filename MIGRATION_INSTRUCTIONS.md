# 📦 Инструкция по миграции данных из poehali.dev в Timeweb

## ✅ Что уже готово
- ✓ SQL-скрипт миграции создан: `timeweb_migration_full.sql`
- ✓ Все 22 функции обновлены с новыми переменными окружения
- ✓ Backend переведен на схему `public` (убраны префиксы `t_p53416936_auxchat_energy_messa`)
- ✓ Frontend настроен на прямые URL функций Yandex Cloud

## 📊 Что будет перенесено

### Таблицы и количество записей:
- **users** - 2 записи (ID: 7, 8)
- **messages** - 95 сообщений в общем чате
- **private_messages** - 74 личных сообщения
- **subscriptions** - 2 подписки
- **user_photos** - 4 фотографии
- **sms_codes** - 4 SMS-кода
- **blacklist** - 0 записей
- **message_reactions** - 0 записей
- **reactions** - 0 записей

**Всего: 177 записей**

---

## 🚀 Шаги миграции

### Вариант 1: Через pgAdmin (рекомендуется)

1. **Откройте pgAdmin 4**

2. **Подключитесь к базе Timeweb:**
   - Host: `f54d84cf7c4086988278b301.twc1.net`
   - Port: `5432`
   - Database: `default_db`
   - Username: `gen_user`
   - Password: `K=w7hxumpubT-F`
   - SSL Mode: `require`

3. **Откройте Query Tool:**
   - Правой кнопкой мыши на `default_db` → **Query Tool**

4. **Загрузите и выполните скрипт:**
   - Нажмите **File → Open** → выберите `timeweb_migration_full.sql`
   - Нажмите **Execute/Run (F5)**

5. **Проверьте результат:**
   ```sql
   SELECT 'users' as table_name, COUNT(*) as count FROM users
   UNION ALL
   SELECT 'messages', COUNT(*) FROM messages
   UNION ALL
   SELECT 'private_messages', COUNT(*) FROM private_messages
   UNION ALL
   SELECT 'subscriptions', COUNT(*) FROM subscriptions
   UNION ALL
   SELECT 'user_photos', COUNT(*) FROM user_photos
   UNION ALL
   SELECT 'sms_codes', COUNT(*) FROM sms_codes;
   ```

   Ожидаемый результат:
   ```
   users: 2
   messages: 95
   private_messages: 74
   subscriptions: 2
   user_photos: 4
   sms_codes: 4
   ```

---

### Вариант 2: Через psql (командная строка)

```bash
psql "postgresql://gen_user:K=w7hxumpubT-F@f54d84cf7c4086988278b301.twc1.net:5432/default_db?sslmode=require" -f timeweb_migration_full.sql
```

---

### Вариант 3: Через Timeweb веб-интерфейс

1. Зайдите в панель Timeweb
2. Откройте раздел "Базы данных"
3. Выберите вашу БД `default_db`
4. Откройте "phpPgAdmin" или "SQL запросы"
5. Скопируйте содержимое файла `timeweb_migration_full.sql` и выполните

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### 1. Конфликты PRIMARY KEY
Если в базе Timeweb уже есть данные с теми же ID (7, 8 для users), скрипт выдаст ошибку:
```
ERROR: duplicate key value violates unique constraint "users_pkey"
```

**Решение:**
```sql
-- Сначала удалите существующие данные:
TRUNCATE TABLE users CASCADE;
TRUNCATE TABLE messages CASCADE;
TRUNCATE TABLE private_messages CASCADE;
TRUNCATE TABLE subscriptions CASCADE;
TRUNCATE TABLE user_photos CASCADE;
TRUNCATE TABLE sms_codes CASCADE;

-- Затем запустите migration скрипт
```

### 2. Отсутствие таблиц
Если таблицы еще не созданы, сначала создайте структуру БД (схему), затем импортируйте данные.

### 3. Аватары пользователей
В таблице `users` есть большие base64 строки аватаров. Убедитесь, что:
- Поле `avatar_url` имеет тип `TEXT` (не VARCHAR!)
- Размер записи не превышает лимиты БД

---

## 🧪 Проверка после миграции

### 1. Проверка количества записей:
```sql
SELECT 
  'users' as table_name, COUNT(*) as count FROM users
UNION ALL
  SELECT 'messages', COUNT(*) FROM messages
UNION ALL
  SELECT 'private_messages', COUNT(*) FROM private_messages
UNION ALL
  SELECT 'subscriptions', COUNT(*) FROM subscriptions
UNION ALL
  SELECT 'user_photos', COUNT(*) FROM user_photos
UNION ALL
  SELECT 'sms_codes', COUNT(*) FROM sms_codes;
```

### 2. Проверка пользователей:
```sql
SELECT id, username, phone, energy, is_admin, is_banned 
FROM users 
ORDER BY id;
```

Ожидается:
- ID 7: Сергей, +7 (922) 131-63-34, energy=9810, admin=true
- ID 8: Лена, +79999999999, energy=10760, admin=false

### 3. Проверка сообщений:
```sql
-- Общий чат (последние 5)
SELECT id, user_id, text, created_at 
FROM messages 
ORDER BY id DESC 
LIMIT 5;

-- Личные сообщения (последние 5)
SELECT id, sender_id, receiver_id, text, created_at 
FROM private_messages 
ORDER BY id DESC 
LIMIT 5;
```

### 4. Проверка подписок:
```sql
SELECT * FROM subscriptions;
```

Ожидается:
- ID 2: subscriber_id=7, subscribed_to_id=8
- ID 3: subscriber_id=8, subscribed_to_id=7

---

## 🔄 После успешной миграции

### 1. Проверьте авторизацию:
- Зайдите на сайт
- Войдите как пользователь `+7 (922) 131-63-34` (Сергей, ID=7)

### 2. Проверьте общий чат:
- Откройте главную страницу
- Убедитесь, что видны 95 сообщений из истории

### 3. Проверьте личные сообщения:
- Откройте диалог с "Лена" (ID=8)
- Убедитесь, что видны все 74 сообщения

### 4. Проверьте новые сообщения:
- Напишите новое сообщение в общий чат
- Напишите личное сообщение
- Проверьте, что они сохраняются в Timeweb:
```sql
SELECT * FROM messages ORDER BY id DESC LIMIT 1;
SELECT * FROM private_messages ORDER BY id DESC LIMIT 1;
```

---

## 🐛 Возможные проблемы

### Ошибка: "relation does not exist"
**Причина:** Таблицы не созданы  
**Решение:** Сначала создайте структуру БД, затем запустите миграцию

### Ошибка: "duplicate key value"
**Причина:** Записи с такими ID уже существуют  
**Решение:** Используйте `TRUNCATE TABLE` (см. выше) или измените ID в скрипте

### Сообщения не сохраняются после миграции
**Причина:** Frontend все еще использует старый API_BASE  
**Решение:** 
1. Проверьте `src/lib/api.ts` - должно быть `import { FUNCTIONS } from './func2url'`
2. Перезапустите frontend: `bun run dev`
3. Очистите кэш браузера (Ctrl+Shift+R)

### Функции возвращают ошибку "relation does not exist"
**Причина:** В коде функций остались старые префиксы схемы  
**Решение:** Проверьте, что все SQL запросы используют `FROM table_name` без префиксов

---

## 📞 Контакты для помощи
Если что-то пошло не так - напиши мне, разберемся! 🚀
